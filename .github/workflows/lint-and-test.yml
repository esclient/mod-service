name: "lint and test"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    types: [ opened, synchronize, reopened ]

jobs:
  lint-and-test:
    uses: esclient/esclient-workflows/.github/workflows/lint-and-test.yml@main
    with:
      python-version: '3.10.4'
